{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo 'Building PHP app'",
    "startCommand": "php -S 0.0.0.0:${PORT:-8000} -t public"
  },
  "deploy": {
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
